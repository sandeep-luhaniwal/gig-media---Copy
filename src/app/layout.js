import "./globals.css";

export const metadata = {
  title: "GigMedia",
  description: "Generated by GigMedia",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={` antialiased`}>{children}</body>
    </html>
  );
}
